const initialRecordState = {
    goal_record: ""
    }
    
    export default function recordsReducer(state = initialRecordState, action) {
    
        switch(action.type) {
            
    
            default:
                return {...state};
        }
    
      